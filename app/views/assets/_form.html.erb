<%= simple_form_for(@asset) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :asset_type %>
    <%= f.input :asset_id %>
    <%= f.input :properties %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
